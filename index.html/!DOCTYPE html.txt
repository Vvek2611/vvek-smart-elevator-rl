<!DOCTYPE html>
<html lang="mr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Madhur Connect - Vitthalwadi</title>
    <style>
        body { font-family: 'Arial', sans-serif; background-color: #f4f7f6; text-align: center; padding: 20px; }
        .card { background: white; padding: 20px; border-radius: 15px; box-shadow: 0 4px 8px rgba(0,0,0,0.1); max-width: 400px; margin: auto; }
        h2 { color: #0056b3; }
        input, select, button { width: 100%; padding: 12px; margin: 10px 0; border-radius: 8px; border: 1px solid #ddd; box-sizing: border-box; }
        button { background-color: #25D366; color: white; font-weight: bold; border: none; cursor: pointer; }
        .footer { font-size: 12px; color: #888; margin-top: 20px; }
    </style>
</head>
<body>

    <div class="card">
        <h2>आपला मधुर म्हात्रे</h2>
        <p>तक्रार निवारण प्रणाली (D-Block)</p>
        
        <input type="text" id="name" placeholder="तुमचे नाव (Your Name)">
        
        <select id="category">
            <option value="">तक्रार श्रेणी निवडा (Category)</option>
            <option value="Roads">रस्ते (Roads)</option>
            <option value="Water">पाणी (Water)</option>
            <option value="Garbage">कचरा (Garbage)</option>
            <option value="Electricity">वीज (Electricity)</option>
        </select>

        <input type="text" id="issue" placeholder="तक्रार थोडक्यात सांगा (Issue Description)">

        <button onclick="sendComplaint()">WhatsApp द्वारे तक्रार पाठवा</button>
    </div>

    <div class="footer">
        Developed by Vvek (MSc AI) | Office: 9137247214
    </div>

    <script>
        function sendComplaint() {
            const name = document.getElementById('name').value;
            const category = document.getElementById('category').value;
            const issue = document.getElementById('issue').value;
            const officeNumber = "919137247214"; // Updated Office Number

            if(!name || !category || !issue) {
                alert("कृपया सर्व माहिती भरा!");
                return;
            }

            const message = `*नवीन तक्रार*%n*नाव:* ${name}%n*श्रेणी:* ${category}%n*तक्रार:* ${issue}`;
            const whatsappUrl = `https://wa.me/${officeNumber}?text=${message}`;
            
            window.open(whatsappUrl, '_blank');
        }
    </script>
</body>
</html>